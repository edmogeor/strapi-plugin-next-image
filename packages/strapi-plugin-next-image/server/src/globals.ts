// fallow-ignore-file unused-file
// Strapi v5 runtime injects the `strapi` global. This declaration
// prevents implicit `any` on every untyped `strapi.*` access.
declare var strapi: import('@strapi/types').Core.Strapi;
